% ediciones/lupa-bw-muestra.tex -- la muestra gratuita de "Leo con
% lupa", en blanco y negro (lupa-bw.tex): el mismo .tex raíz, con
% \edicion fijado antes (ver "la edición" en preamble.tex, y
% notes/07-ediciones.md). Se compila desde la raíz del repositorio:
% `make build-lupa-bw-muestra`.
\def\edicion{muestra}
\input{lupa-bw}
