% ediciones/palabras-bw-muestra.tex -- la muestra gratuita del cuaderno
% de primeras palabras, en blanco y negro (palabras-bw.tex): el mismo
% .tex raíz, con \edicion fijado antes (ver "la edición" en
% preamble.tex, y notes/07-ediciones.md). Se compila desde la raíz del
% repositorio: `make build-palabras-bw-muestra`.
\def\edicion{muestra}
\input{palabras-bw}
