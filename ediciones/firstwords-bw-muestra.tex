% ediciones/firstwords-bw-muestra.tex -- la muestra gratuita de «First
% Words», en blanco y negro (firstwords-bw.tex): el mismo .tex raíz, con
% \edicion fijado antes (ver "la edición" en preamble.tex, y
% notes/07-ediciones.md). Se compila desde la raíz del repositorio:
% `make build-firstwords-bw-muestra`.
\def\edicion{muestra}
\input{firstwords-bw}
