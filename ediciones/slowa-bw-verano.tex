% ediciones/slowa-bw-verano.tex -- el cuaderno de verano de «Pierwsze słowa»,
% el cuaderno de primeras palabras en polaco, en blanco y negro (slowa-bw.tex):
% el mismo .tex raíz, con \edicion fijado antes (ver "la edición" en
% preamble.tex, y notes/07-ediciones.md). Se compila desde la raíz del
% repositorio: `make build-slowa-bw-verano`.
\def\edicion{verano}
\input{slowa-bw}
